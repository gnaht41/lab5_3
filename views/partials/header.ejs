<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= title || 'My App' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
    <div class="container">
      <a class="navbar-brand" href="/">My CRUD App</a>
      <div>
        <% if (user) { %>
        <a class="btn btn-primary me-2" href="/suppliers">Suppliers</a>
        <a class="btn btn-success me-2" href="/products">Products</a>
        <span class="text-light me-2">Hello, <%= user.username %></span>
        <a class="btn btn-outline-light btn-sm" href="/auth/logout">Logout</a>
        <% } else { %>
        <a class="btn btn-outline-light btn-sm me-2" href="/auth/login">Login</a>
        <a class="btn btn-outline-light btn-sm" href="/auth/register">Register</a>
        <% } %>
      </div>
    </div>
  </nav>
  <div class="container">